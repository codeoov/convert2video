{
  "name": "c2v-desktop",
  "version": "0.0.1",
  "description": "C2V Desktop Recorder — setup: cd desktop && npm install && npm run dev",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
