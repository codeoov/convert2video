package com.example.convert2video.ui.theme

import androidx.compose.ui.graphics.Color

// Honey yellow + deep cocoa palette — mirrors .claude/claude-design/MusicToVideo App.dc.html

object C2vDark {
    val bg = Color(0xFF16120E)
    val surface = Color(0xFF211A15)
    val surface2 = Color(0xFF2B221B)
    val line = Color(0xFF3A2F26)
    val ink = Color(0xFFF6EFE4)
    val ink2 = Color(0xFFBCAB98)
    val ink3 = Color(0xFF7E6C59)
    val accent = Color(0xFFFFC93C)
    val accentInk = Color(0xFF2A1C05)
    val accentSoft = Color(0x24FFC93C) // rgba(255,201,60,.14)
    val danger = Color(0xFFFF8A6B)
    val ok = Color(0xFF7FD1A4)
    val scrim = Color(0x80000000)
    /** 에러 로그 W 배지용 경고 색상 토큰 */
    val warningContainer = Color(0xFF3A2800) // 어두운 앰버 컨테이너
    val onWarningContainer = Color(0xFFFFCB6B) // 밝은 앰버 텍스트
}

object C2vLight {
    val bg = Color(0xFFF1E7D5)
    val surface = Color(0xFFFFFCF5)
    val surface2 = Color(0xFFF0E4CF)
    val line = Color(0xFFE1D3B9)
    val ink = Color(0xFF2A2019)
    val ink2 = Color(0xFF6E5D49)
    val ink3 = Color(0xFF9C8B78)
    val accent = Color(0xFFF7B500)
    val accentInk = Color(0xFF2A1C05)
    val accentSoft = Color(0x29F7B500) // rgba(247,181,0,.16)
    val danger = Color(0xFFC1492A)
    val ok = Color(0xFF2E8C63)
    val scrim = Color(0x80000000)
    /** 에러 로그 W 배지용 경고 색상 토큰 */
    val warningContainer = Color(0xFFFFF3D4) // 밝은 피치 컨테이너
    val onWarningContainer = Color(0xFF7A4500) // 어두운 앰버 텍스트
}
